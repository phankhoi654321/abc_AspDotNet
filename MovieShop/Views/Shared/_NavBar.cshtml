<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
    <a class="navbar-brand" href="/Home">Movie Rent</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            @{
                var newClassMovies = ViewContext.RouteData.Values["controller"].ToString() == "Movies" ? "active" : null;
            }
            <li class="nav-item @newClassMovies">
                @Html.ActionLink("Movies", "Index", "Movies", new { area = "" }, new { @class = "nav-link" })
            </li>
            @{
                var newClassCustomer = ViewContext.RouteData.Values["controller"].ToString() == "Customers" ? "active" : null;
            }
            <li class="nav-item @newClassCustomer ">
                @Html.ActionLink("Customers", "Index", "Customers", new { id = "wasClick" }, new { @class = "nav-link" })
            </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="text" placeholder="Search">
            <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
        </form>
    </div>
</nav>